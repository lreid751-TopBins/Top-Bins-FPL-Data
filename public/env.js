/*
 * Points the front end at the deployed Supabase Function.
 * The Pages workflow overwrites this file at build time; leaving it blank
 * locally makes the app talk to the mock server on the same origin.
 */
window.__FPL_API_BASE__ = "";
